
# MongoDB Connection String - for demonstration only, cannot be used directly in browser
# In production, you would need a backend API service to connect to MongoDB
VITE_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# MongoDB Database Name
VITE_MONGODB_DB_NAME=lyzLawFirm
